# Как обновить функцию setup-database в Yandex Cloud

## Проблема
Функция возвращает "Hello World!" вместо ожидаемого JSON ответа. Это значит, что используется старый код.

## Решение: Обновить код функции

### Шаг 1: Открой функцию в консоли
1. Открой https://console.cloud.yandex.ru/functions
2. Найди функцию `setup-database`
3. Нажми на неё, чтобы открыть

### Шаг 2: Обнови код функции
1. Перейди на вкладку **"Редактор"** или **"Код"**
2. Скопируй весь код из файла `backend/setup-database/index.py` (он уже обновлён с поддержкой ключа сервисного аккаунта)
3. Вставь код в редактор функции
4. Сохрани изменения

### Шаг 3: Обнови зависимости
1. Перейди на вкладку **"Зависимости"** или **"Requirements"**
2. Убедись, что там есть следующие строки:
   ```
   psycopg2-binary>=2.9.0
   requests>=2.31.0
   cryptography>=41.0.0
   PyJWT>=2.8.0
   ```
3. Если чего-то не хватает, добавь недостающие зависимости
4. Сохрани изменения

### Шаг 4: Проверь переменные окружения
1. Перейди на вкладку **"Конфигурация"** или **"Переменные окружения"**
2. Убедись, что добавлены две переменные:
   - `YANDEX_SERVICE_ACCOUNT_KEY_ID` = `ajeke7hvv73d03siopbo`
   - `YANDEX_SERVICE_ACCOUNT_PRIVATE_KEY` = (весь твой приватный ключ)
3. Если переменных нет, добавь их

### Шаг 5: Создай новую версию функции
1. После сохранения всех изменений нажми кнопку **"Создать версию"** или **"Deploy"**
2. Дождись завершения создания версии (обычно 1-2 минуты)

### Шаг 6: Проверь работу функции
После создания новой версии протестируй функцию:

```bash
curl -X POST https://functions.yandexcloud.net/d4e1b7fvqqmku2er9rav \
  -H "Content-Type: application/json" \
  -d '{"db_name": "test_db", "db_user": "test_user"}'
```

**Ожидаемый результат:**
- Должен вернуться JSON с полем `logs` (массив строк с логами)
- В логах должно быть: "🔐 Получаю IAM токен..."
- Если всё настроено правильно, функция начнёт создавать VM

## Если что-то не работает

### Ошибка: "Не настроен YANDEX_CLOUD_TOKEN или ключи сервисного аккаунта"
- Проверь, что переменные окружения `YANDEX_SERVICE_ACCOUNT_KEY_ID` и `YANDEX_SERVICE_ACCOUNT_PRIVATE_KEY` добавлены
- Убедись, что значения скопированы правильно (без лишних пробелов)

### Ошибка: "Библиотека jwt не установлена"
- Проверь, что в зависимостях есть `PyJWT>=2.8.0`
- Создай новую версию функции после добавления зависимости

### Ошибка: "IAM token error: 401"
- Попробуй использовать ID сервисного аккаунта вместо ID ключа в переменной `YANDEX_SERVICE_ACCOUNT_KEY_ID`
- Найди ID сервисного аккаунта в консоли: IAM → Сервисные аккаунты

### Функция всё ещё возвращает "Hello World!"
- Убедись, что ты создал новую версию функции после обновления кода
- Проверь, что используется правильная версия функции (в URL функции должна быть указана актуальная версия)

## Готово! 🎉

После обновления функции она должна работать с ключом сервисного аккаунта и создавать VM с PostgreSQL.
