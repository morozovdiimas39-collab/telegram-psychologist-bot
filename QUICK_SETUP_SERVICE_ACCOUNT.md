# Быстрая настройка функции setup-database с ключом сервисного аккаунта

## Что нужно сделать

### 1. Открой функцию setup-database в Yandex Cloud Functions
https://console.cloud.yandex.ru/functions

### 2. Добавь две переменные окружения:

#### Переменная 1: `YANDEX_SERVICE_ACCOUNT_KEY_ID`
**Значение:** `ajeke7hvv73d03siopbo` (твой ID ключа)

#### Переменная 2: `YANDEX_SERVICE_ACCOUNT_PRIVATE_KEY`
**Значение:** Весь твой приватный ключ (от `-----BEGIN PRIVATE KEY-----` до `-----END PRIVATE KEY-----`)

**Важно:** Скопируй ВЕСЬ блок ключа, включая строки `-----BEGIN PRIVATE KEY-----` и `-----END PRIVATE KEY-----`

### 3. Сохрани и создай новую версию функции

### 4. Проверь работу

Вызови функцию и проверь логи. Должно быть:
- ✅ "🔐 Получаю IAM токен..."
- ✅ "Использую ключ сервисного аккаунта..."
- ✅ "✅ JWT токен создан"

## Если получил ошибку 401

Это значит, что ID ключа не подходит. Нужно использовать **ID сервисного аккаунта** вместо ID ключа.

**Как найти ID сервисного аккаунта:**
1. Открой https://console.cloud.yandex.ru/cloud
2. Перейди в раздел **"Сервисные аккаунты"** (IAM → Сервисные аккаунты)
3. Найди нужный сервисный аккаунт
4. Скопируй его **ID** (не имя, а именно ID)
5. Используй этот ID в переменной `YANDEX_SERVICE_ACCOUNT_KEY_ID`

## Готово! 🎉

Теперь функция `setup-database` будет работать с твоим ключом сервисного аккаунта.
